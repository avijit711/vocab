{
  "name": "android-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "npx tailwindcss -i www/css/input.css -o www/css/app.css --minify",
    "build:web": "npm run build:css",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "build:android": "npm run build:web && npm run cap:sync && cd android && ./gradlew assembleRelease"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "tailwindcss": "^3.4.17"
  }
}
